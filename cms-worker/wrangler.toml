name = "cms-auth"
main = "index.js"
compatibility_date = "2024-01-01"

# After deploying, note the worker URL shown in the dashboard
# (https://cms-auth.YOUR_SUBDOMAIN.workers.dev) and paste it
# into admin/config.yml as the base_url value.
